export default function ({ store, redirect, route }) {
  //  if(store.state.authUser === null && route.name !== 'login' && route.name !== 'signup'){
  //    redirect('/login')
  //  }
  //  if(store.state.authUser !== null && (route.name === 'login' || route.name === 'signup')){
  //    redirect('/users/' + store.state.authUser)
  //  }
  }
  
  // need to edit admin root meaning '/'